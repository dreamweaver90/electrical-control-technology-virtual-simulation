# 虚拟接线仿真系统 — 配置说明（二次开发，v3）

本文件只讲**二次开发**（配置层）：**不改引擎，只写配置**即可新增器件、绑定关系、动画、故障点与教学场景；需要新的电气/气动行为时才写器件类（契约见 §十四、内置类清单见 §十五）。

> **文档分工**：`CLAUDE.md` 描述整体项目架构、设计与使用（运行方式、界面、双层网络模型、器件类体系、关键约定）；本文件描述二次开发（字段清单、可配置功能、配置效果与案例）。查"系统怎么设计的"看 `CLAUDE.md`，查"这个器件该怎么写"看本文件。

- 配置入口：`system_config_v3.json`（系统级 + 器件/场景文件名列表）
- 器件配置：`components/<id>.json`（电气 `electrical/`、气动 `pneumatic/`、机械 `mechanical/`）
- 场景配置：`scenes/*.json`
- 解析实现：`js/config.js`（`ComponentDefinition`）

---

## 一、快速上手：新增一个器件（七步）

1. **复制最接近的现有配置**（如做按钮 → 抄 `components/electrical/button_no_nc_independent.json`），文件名为 `<id>.json`；
2. **改顶层标识**：`id`（唯一）、`class`（器件类名，内置类见 §十五）、`name`、`category`（元件库分组）、`size`（卡片尺寸，也是端子坐标基准）；
3. **准备图形**：PNG 放 `images/` 目录，`image[0]` 写 `<div><img src='images/图.png' width='W' height='H'></div>`；复杂/可变色图形用**内联 SVG**（viewBox = size，内部坐标 = 端子坐标）；
4. **定义端子**：`terminals.electrical.pins`（或 `pneumatic.pins`）+ 四类端子对条目（`contacts`/`loads`/`permanent`/`sensors`）；
5. **定义参数**：`params`（铭牌定值 / `adjustable` 可调 / `readonly` 只读显示）；
6. **加交互与外观**（可选）：`zones`（点击热区）、`bind`（图层显隐）、`classBind`（换元素 class）、`anims`（动画）、`display`（图上文本）；
7. **注册**：把 `"electrical/xx.json"` 加进 `system_config_v3.json` 的 `components` 数组 → 刷新页面，元件库出现新器件。

> 只有需要**新的电气/气动行为**（新的求解方式、新的状态机）时才写器件类，配置能表达的一律写在 JSON 里。

---

## 二、系统配置 `system_config_v3.json`

| 字段 | 说明 |
|---|---|
| `simulation.refreshIntervalMs` | 仿真周期（ms），默认 50。电机加减速、时序计时等都按它换算 |
| `canvas.backgroundColor` | 画布背景色（注入 CSS 变量 `--panel-bg`） |
| `canvas.minZoom`/`maxZoom` | 缩放范围 |
| `canvas.canvasWidth` | 有线槽模式画布宽度上限 |
| `canvas.ductRows.{initial,max}` | 有线槽初始行数 / 最大行数（行满自动加行） |
| `canvas.freeLayout.{widthScale,heightScale,minWidth,minHeight,growFactor}` | 无线槽操作区尺寸：初始 = `max(视口×倍率, min*)`；不足时按 `growFactor` 整幅扩充 |
| `wireDuct.{slotWidth,slotHeight,color,borderColor}` | 线槽外观 |
| `wiring.electrical.{thickness,colors[],defaultColor,labelFontSize,labelGap}` | 导线线宽/可选颜色/默认色/线标字号/**线标距端子内缩**（缺省 12px） |
| `wiring.pneumatic.{thickness,colors[],defaultColor,labelFontSize,labelGap}` | 气管同上（气管恒悬链线、canvas 三层描边） |
| `render.terminalHoverColor`/`terminalSelectedColor` | 端子悬停/选中高亮（注入 `--term-*` CSS 变量） |
| `render.hoverOpacityUnrelated`/`hoverOpacitySameGroup` | 悬停导线时其它导线的淡化透明度 |
| `multimeter.{redProbe,blackProbe}` | 表笔图片与笔尖坐标 `{fileName,height,tipX,tipY}`（tipX/tipY 为目标尺寸像素；`fileName` 同样写 `images/红表笔.png` 这种相对站点根路径，**不能以 `/` 开头**） |
| `rules.defaultConnectLimit` | 端子缺省允许接线数（缺省 2） |
| `rules.tripCurrent` | 断路器跳闸电流阈值（A） |
| `rules.faultLevels` | 排故难度名数组，**下标即等级阈值**（现 `["简单","中等","困难"]`） |
| `undo.maxSteps` | 撤回栈容量（缺省 50） |
| `components[]` | 器件配置文件名列表（相对 `components/`） |
| `scenes[]` | 场景文件名列表（相对 `scenes/`） |

主题注入：`canvas.backgroundColor`、`render.terminal*` 由 `App._applyTheme()` 转成 `:root` 上的 CSS 变量，CSS 带默认兜底——未配即保持默认视觉。

---

## 三、器件配置 `components/<id>.json` 全字段

```jsonc
{
  "id": "schneider_contactor",       // 必填：唯一标识（拖放/单例判定）
  "class": "CoilDeviceInstance",     // 必填：器件类（内置清单见 §十五）
  "category": "继电器&接触器",        // 元件库分组标签（缺省 "其他"）
  "name": "交流接触器",               // 显示名（删除确认/绑定提示/告警文案）
  "description": "线圈 380V AC 三主触点",  // 悬停说明
  "tags": ["接触器", "AC"],
  "singleton": false,                // true = 全盘只能放一个（如三相电源、十字工作台）
  "size": { "width": 192, "height": 300 },   // 卡片尺寸 = 端子/图形坐标基准
  "terminalHitRadius": 14,           // 端子热区半径（缺省 12）
  "thumbnail": "<div>…</div>",       // 元件库缩略图（缺省取 image[0]）
  "image": [ … ],                    // 图层与绑定（§四）
  "zones": [ … ],                    // 交互热区（§五）
  "params": [ … ],                   // 参数容器（§六）
  "terminals": { "electrical": { … } },      // 端子（§七）
  "source": { … },                   // 电源 EMF 定义（§九）
  "input":  { … },                   // 整流器/变压器输入判定（§九）
  "speedRelay": [ … ],               // 电机内置速度继电器触点（§十一）
  "pneumaticSource": { … },          // 气源出气端（§十）
  "regulator": { … },                // 调压阀进出气端（§十）
  "pneumatic": { "cylinders": [ … ], "valves": [ … ] }   // 气缸单元 / 双线圈阀（§十）
}
```

---

## 四、图像与外观绑定（都绑定 `params`）

### 4.1 `image` 图层

```jsonc
"image": [
  { "id": "default",  "x": 0, "y": 0, "string": "<div><img src='images/接触器.png' width='192' height='300'></div>" },
  { "id": "断电",     "x": 0, "y": 0, "string": "…", "bind": "!energized" },
  { "id": "得电",     "x": 0, "y": 0, "string": "…", "bind": "energized" }
]
```

- `id === "default"` 常显于底层（也可不叫 default：**未写 `bind` 的图层恒显**）；数组下标 = 层叠顺序（后者在上）；
- `x/y` 为图层相对卡片的偏移（一般 0/0）；
- `string` = HTML 字符串：可 `<img>`、可内联 `<svg>`。**SVG 用器件全尺寸 viewBox，内部坐标与端子坐标一致**。
- **图片路径**：全部图片集中放在 `images/`，`string` 里一律写 `images/<文件名>.png`。`string` 是**注入到页面 DOM 的 HTML**，相对路径按**页面 URL（= 站点根）**解析，与 JSON 文件所在目录无关；因此不要写 `../图.png`，也不要写以 `/` 开头的绝对路径（部署到子目录时失效）。

### 4.2 `bind` — 图层显隐（truthy/falsy）

```jsonc
{ "id": "闭合", "bind": "pushed" }    // params.pushed 为真时显示
{ "id": "断开", "bind": "!pushed" }   // 取反（! 前缀）
```

- 支持**表达式**：`"energized && !locked"`、`"!(a || b)"`（`!` > `&&` > `||`，可用括号）；
- 常见状态键：`pushed`（按钮）、`switch`（开关/断路器）、`energized`（线圈）、`tripped`（热继电器跳闸）、`cw`/`ccw`（电机转向）、`stalled`（堵转）、`lit`（指示灯）、`actuated`（行程开关）、`fuse_L1`（熔断）；
- **同一个键可绑多个图层**（正/反两层做完整状态图）。

### 4.3 `classBind` — 参数值 → 元素 class（换整套样式）

```jsonc
{ "id": "复位", "x": 0, "y": 0,
  "string": "<svg …><rect data-bind='cap' class='cap-red' …/></svg>",
  "classBind": { "cap": "capColor" } }     // params.capColor 的值直接作为该元素的 class
```

```jsonc
"params": [
  { "id": "capColor", "label": "钮帽颜色", "type": "select", "adjustable": true, "default": "cap-red",
    "options": [ { "value": "cap-red", "label": "红" }, { "value": "cap-yellow", "label": "黄" },
                 { "value": "cap-blue", "label": "蓝" }, { "value": "cap-green", "label": "绿" },
                 { "value": "cap-white", "label": "白" }, { "value": "cap-black", "label": "黑" } ] }
]
```

```css
/* css/style.css：样式按 class 名定义，可换颜色/形状/阴影等任意样式 */
.cap-red { fill: #e74c3c; }  .cap-yellow { fill: #f1c40f; }  .cap-blue { fill: #3498db; }
.cap-green { fill: #2ecc71; } .cap-white { fill: #ecf0f1; }  .cap-black { fill: #34495e; }
```

- 值即 class 名，**同名 `data-bind` 元素全部生效**（松开/按下两个图层的钮帽同时换色）；
- 用途不止颜色：一套 class 可切换形状、材质、按下效果（CSS 里随便写）。

### 4.4 `anims` — 元素动画（数值插值 / 端子坐标同步）

```jsonc
"anims": {
  "needle": {                                     // 键 = 元素 data-bind 标记名
    "source": "needle_pos",                       // 绑定参数键
    "map": { "min": "-param:range", "max": "param:range" },   // 值域：数字 或 "param:key"（取负 = 对称量程）
    "move":   { "from": [60, 20], "to": [60, 180] },          // ① 直线两点式 → transform translate
    "rotate": { "center": [70, 110], "from": -180, "to": 0 }, // ② 绕点扫角 → transform rotate
    "attr":   { "style.opacity": [0.15, 1] },                 // ③ 数值属性插值（style. 前缀写 CSS）
    "terminal": { "id": "t1", "move": { "from": [60, 20], "to": [60, 180] } },  // 端子坐标同步（可选）
    "smooth": true                                // 可选：CSS transition 补间
  }
}
```

- 每 tick 刷新：`t = (params[source] − map.min) / (map.max − map.min)`（clamp 0~1）→ 插值 from→to；
- 源值为 null → 不触碰元素（保持 SVG 写死的初始态）；`map` 缺省 0~100；
- **`terminal` 端子同步**：坐标写入 `Terminal.x/y` + 圆点跟随；scan 末尾检测到移动 → **自动重算 auto 导线路径**（结构层不读坐标，电气逻辑零影响）；
- `move` 只写 `from≠to` 的轴 → 十字滑台两条 bind 互不覆盖；`rotate` 的半径由配置坐标反推、角度为绝对角。

**动画用法示例（5 例）**

**例 A｜直线动画（纯视觉）**
```jsonc
"image": [{ "id": "default", "x": 0, "y": 0,
  "string": "<div><svg width='120' height='200' viewBox='0 0 120 200'><g data-bind='car' transform='translate(60 20)'><rect x='-8' y='-6' width='16' height='12' fill='#333'/></g></svg></div>",
  "anims": { "car": { "source": "posY", "map": { "min": 0, "max": 100 }, "move": { "from": [60, 20], "to": [60, 180] } } } }]
```

**例 B｜直线动画 + 端子同步（滑触头，导线跟随）**
```jsonc
"terminals": { "electrical": {
  "pins": { "t1": { "x": 60, "y": 20, "dir": "right", "tip": "滑触头" } },
  "permanent": { "j": { "pairs": [["t1", "t1"]] } } } },
"image": [{ "id": "default", "x": 0, "y": 0,
  "string": "<div><svg width='120' height='200' viewBox='0 0 120 200'><rect x='50' y='14' width='20' height='172' fill='#ddd'/><g data-bind='slider' transform='translate(60 20)'><rect x='-6' y='-6' width='12' height='12' fill='#333'/></g></svg></div>",
  "anims": { "slider": { "source": "position", "map": { "min": 0, "max": 100 },
    "move": { "from": [60, 20], "to": [60, 180] },
    "terminal": { "id": "t1", "move": { "from": [60, 20], "to": [60, 180] } } } } }]
```

**例 C｜旋转动画（指针）**
```jsonc
"anims": { "hand": { "source": "angle", "map": { "min": 0, "max": 100 }, "rotate": { "center": [70, 70], "from": -90, "to": 90 } } }
```

**例 D｜旋转 + 端子（旋转触头，导线沿弧跟随）**
```jsonc
"anims": { "hand": { "source": "angle", "map": { "min": 0, "max": 100 },
  "rotate": { "center": [70, 70], "from": -90, "to": 90 },
  "terminal": { "id": "t1", "rotate": { "center": [70, 70], "from": -90, "to": 90 } } } }
```

**例 E｜十字滑台（两参数分轴，视觉嵌套 `<g>`）**
```jsonc
"terminals": { "electrical": { "pins": { "t1": { "x": 60, "y": 60, "dir": "right" } },
  "permanent": { "j": { "pairs": [["t1", "t1"]] } } } },
"image": [{ "id": "default", "x": 0, "y": 0,
  "string": "<div><svg width='200' height='200' viewBox='0 0 200 200'><g data-bind='axisX' transform='translate(60 60)'><g data-bind='axisY'><rect x='-10' y='-10' width='20' height='20' fill='#333'/></g></g></svg></div>",
  "anims": {
    "axisX": { "source": "posX", "map": { "min": 0, "max": 100 }, "move": { "from": [60, 60], "to": [160, 60] },
               "terminal": { "id": "t1", "move": { "from": [60, 60], "to": [160, 60] } } },
    "axisY": { "source": "posY", "map": { "min": 0, "max": 100 }, "move": { "from": [60, 60], "to": [60, 160] },
               "terminal": { "id": "t1", "move": { "from": [60, 60], "to": [60, 160] } } } } }]
```

### 4.5 `display` — 参数值叠加文本

```jsonc
{ "id": "ratedSpeed", "type": "number", "default": 1450, "adjustable": true,
  "display": { "x": 100, "y": 60, "suffix": " rpm", "decimals": 0, "class": "speed-label" } }
```

| display 字段 | 说明 |
|---|---|
| `x`/`y` | 文本**中心**坐标（器件坐标系） |
| `fontSize`/`color` | 内联字号/颜色（可选） |
| `class` | 引用 CSS 类（复杂样式，如 `.comp-text-label.speed-label` 白底加粗、`.yellow-plate` 黄底黑字标牌、`.red-plate` 红底白字、`.stall-label` 红字白描边） |
| `prefix`/`suffix` | 前后缀（如 `" rpm"`、`" A"`、`"%"`） |
| `decimals` | 数字小数位 |
| `scale` | 数值倍率（如位置 0~1 显示成 0~100 → `scale:100`） |
| `trueText`/`falseText` | bool 参数的真/假文案（如 `trueText:"堵转"`）；**false 且未配 falseText → 不显示** |
| `when` | **显示条件**（支持表达式，如 `"speed"`、`"switch"`、`"pos > 0.5"` 之外的比较请改用派生键） |

格式化规则：`number` → `prefix + (v×scale).toFixed(decimals) + suffix`；`select` → 选项 label；`bool` → trueText/falseText；`text` → 原样。未配 `display` 的参数不在图上显示。

**案例**：
```jsonc
// ① 电机转速（0 时不显示）
{ "id": "speed", "display": { "x": 80, "y": 55, "class": "speed-label", "suffix": " rpm", "decimals": 0, "when": "speed" } }
// ② 电机堵转字样（与转速同位，speed=0 时转速自动隐藏、由它接替）
{ "id": "stalled", "type": "bool", "display": { "x": 80, "y": 55, "fontSize": 22, "class": "stall-label", "trueText": "堵转", "when": "stalled" } }
// ③ 名称标牌（黄底黑字）
{ "id": "name", "type": "text", "adjustable": true, "display": { "x": 96, "y": 192, "fontSize": 16, "class": "yellow-plate" } }
```

### 4.6 电表读数

SVG 中放 `<text data-mv='1'>---</text>`（可多个，按 `data-mv` 区分），器件类扫描时自动写读数（电压/电流/电能表 `p`/`e`）。

---

## 五、`zones` — 交互热区

```jsonc
"zones": [
  { "id": "sb",     "event": "press", "action": "toggle", "state": "pushed", "tips": "自复位按钮", "x": 82, "y": 23, "r": 13 },
  { "id": "handle", "event": "click", "action": "toggle", "state": "switch", "tips": "合闸/分闸", "x": 98, "y": 136, "r": 40 },
  { "id": "fuse1",  "event": "click", "action": "reset",  "state": "fuse_L1",  "tips": "更换熔断器", "x": 35, "y": 144, "r": 18 }
]
```

| 字段 | 说明 |
|---|---|
| `x`/`y`/`r` | 圆形热区（圆心 + 半径，器件坐标） |
| `event` | `"click"`（默认，点一下触发）或 `"press"`（按下/松开两阶段，自复位按钮用） |
| `action` | 内置：`toggle`（翻转）/ `on` / `off`（置位/复位，`down` 阶段生效）/ `press`（跟随按下松开）；**自定义**：由器件类 `handleZoneAction(id, action, phase)` 处理 |
| `state` | 带该字段时，内置 action 自动读写 `params[state]`（触点 `when` 即可引用该键） |
| `tips` | 悬停提示文本 |

**自定义 action 的现有实现（照抄即可）**：

| action | 器件 | 效果 |
|---|---|---|
| `reset` | 熔断器 `FuseInstance` | 逐相复位熔断键（更换熔丝） |
| `fuse` | 整流器/变压器 | 复位短路锁死 |
| `view3d` | 十字工作台 `SlideTableInstance` | 开/关 3D 悬浮视窗 |

**案例：自复位按钮**（点下 → `pushed=true`，松开 → `false`）
```jsonc
"zones": [{ "id": "sb", "event": "press", "action": "toggle", "state": "pushed", "x": 82, "y": 23, "r": 13, "tips": "自复位按钮" }],
"terminals": { "electrical": { "contacts": {
  "no": { "pairs": [["no_1_u","no_1_d"]], "when": "pushed",  "closeDelay": 1, "openDelay": 0 },
  "nc": { "pairs": [["nc_1_u","nc_1_d"]], "when": "!pushed", "closeDelay": 1, "openDelay": 0 } } } }
```

---

## 六、`params` — 参数容器

一切状态与数值都存在 `inst.params`：铭牌定值、面板可调项、只读派生值、逻辑状态键、运行时值。

```jsonc
"params": [
  { "id": "coilVoltage", "default": 380 },                                    // 定值（铭牌，不进面板）
  { "id": "impedance", "default": { "re": 730, "im": 6300 } },                // AC 阻抗（对象=定值复阻抗）
  { "id": "dcResistance", "default": 730 },                                   // 直流电阻 / 欧姆档（缺省 = impedance.re）
  { "id": "delayTime", "label": "延时时间", "type": "number", "unit": "s",
    "min": 0.1, "max": 30, "step": 0.1, "default": 5, "adjustable": true },   // 可调
  { "id": "ratedCurrent", "label": "额定电流", "type": "number", "unit": "A",
    "decimals": 2, "readonly": true },                                        // 只读显示（值由器件类算）
  { "id": "target", "label": "执行机构", "type": "bind", "allow": ["relative-position"], "adjustable": true }
]
```

| 字段 | 说明 |
|---|---|
| `id` | 参数键（`bind`/`when`/`state`/`anims.source`/`display` 都引用它） |
| `default` | 默认值（缺省：bool→false、其余→null） |
| `type` | `number`（配合 `unit/min/max/step`）/ `select`（`options`）/ `bool` / `text` / `bind` |
| `adjustable` | `true` = 右键面板**显示且可编辑** |
| `readonly` | `true` = 右键面板**只读显示**（值由器件类写入 params，如电机额定/堵转电流）；配 `label`/`unit`/`decimals` |
| `options` | select 选项：`[{value,label}]`，或动态字符串 `"bound:<bind参数id>.<方法名>"`（见下） |
| `allow` | bind 参数：可绑定目标须具备的**能力名**列表（§十四） |

**面板可见规则**：`adjustable ∨ readonly`（`ComponentDefinition.panelParams()`）。

**参数写回**：面板"确定" → 写回 `inst.params` → 调用 `onParamsChanged()`（器件类重算派生值）→ 刷新显示。

### 6.1 阻抗三处来源（交流 / 直流 / 欧姆档）

| 场景 | 取值来源 | 说明 |
|---|---|---|
| **AC 岛**（交流求解） | `params.impedance` | **数字** = 纯电阻且**可面板调**（如电阻器）；**对象 `{re,im}`** = 定值复阻抗（如线圈 730+j6300）；`computed` 条目走器件类钩子（`"motor"`/`"reflect"`） |
| **DC 岛**（直流求解） | `params.dcResistance` → 缺省回退 `impedance.re` | 直流稳态感抗为零，纯电阻器件无需配（自动跟随 `impedance`） |
| **欧姆档**（万用表电阻档） | 同上（`dcResistance`） | 测的是直流电阻，与运行态解耦 |
| `contacts` / `sensors` | `params.contactResistance` / `params.sensorResistance` → 兜底条目 `resistance` | 触头电阻、分流器/热元件小阻值 |

> 统一入口是基类 `_branchZ(cat, entry)`：**器件级 params 优先、条目值兜底**；同一类别内共享一个值。
> 纯电阻器件**不要配 `dcResistance`**——留空即自动等于当前阻值（改了 `impedance` 欧姆档跟着变）。

### 6.2 `bind` 参数（器件间绑定）

```jsonc
// 磁性传感器：绑定气缸（relative-position 能力），再选具体单元
{ "id": "target", "label": "绑定气缸", "type": "bind", "allow": ["relative-position"], "adjustable": true },
{ "id": "unit",   "label": "检测单元", "type": "select", "options": "bound:target.relativePositions",
  "default": 0, "adjustable": true }
```

- 面板点"绑定"→ 点选盘上具备该能力的器件（不满足能力会 toast 提示）；
- **动态下拉**：`options: "bound:<bind参数id>.<目标方法名>"` → 调目标实例的方法生成选项（`relativePositions()` 返回 `[{label,posKey,position}]` → 下拉显示各单元 label，value = 下标）；
- 运行期 `params.target` = 目标 `instanceId`；**持久化时自动改写为 `"@组件数组索引"`**（场景/工程文件里写 `"@2"` 即绑定 components 数组第 3 个器件）；
- 器件类里读取：`GS.panel.instances.get(this.params.target)`，再用能力契约方法取值（如 `getSpeed()`、`relativePositions()`）。

---

## 七、`terminals` — 端子定义

```jsonc
"terminals": {
  "electrical": {                       // 介质分组：electrical | pneumatic（不可互接）
    "pins": { "a1": { "x": 162, "y": 69, "dir": "up", "tip": "A1线圈" } },
    "contacts": { … }, "loads": { … }, "permanent": { … }, "sensors": { … }
  }
}
```

**`pins` 属性**

| 属性 | 说明 |
|---|---|
| `x`/`y` | 端子坐标（= `size` 坐标系，与图形对齐） |
| `dir` | 出线方向 `up`/`down`/`left`/`right`（决定导线离开端子的首段方向） |
| `tip` | 排故显示名（缺省用 id） |
| `isHidden` | 隐藏端子（不显示/不可点/不可测） |
| `connectLimit` | 允许接线数（缺省取 `rules.defaultConnectLimit`）；**0 = 只能测量、不可接线**（万用表专用端子） |
| `allowLiveConnect` | **允许带电接线**（默认 false；电源出线、变压器初级等需配 true） |
| `faultLevel` | 导线接线故障等级（§八） |

**四类端子对条目**

**① `contacts` 触点对** —— 状态驱动通断
```jsonc
"contacts": {
  "main_no": {
    "pairs": [["main_1_u","main_1_d"], ["main_2_u","main_2_d"]],
    "when": "energized",              // 闭合条件（支持表达式；"!energized" = NC）
    "closeDelay": 1, "openDelay": 0,  // 断开→闭合 / 闭合→断开 延迟（tick 数，默认 0）
    "resistance": 0.001,              // 可选：触头电阻（数值层）
    "fault": { "level": 2, "types": ["open","stuck"] }
  }
}
```
- `when` 缺省 → 基类不自动判定，由器件类 `closedContactPairs()` 实现；
- 接触器类器件的"NC 先动、NO 后动"就是 `closeDelay:1 / openDelay:0` + `when` 取反配出来的。

**② `loads` 阻抗负载** —— 消耗功率的支路
```jsonc
"loads": {
  "coil1": { "pins": ["a1","a2"], "state": "energized", "fault": { "level": 1, "types": ["open"] } },
  "u":     { "pins": ["U1","U2"], "computed": "motor",   "fault": { "level": 1, "types": ["open"] } },
  "refl":  { "pins": ["p1","p2"], "computed": "reflect", "fault": { "level": 10 } }
}
```
- **条目不写阻抗**：AC 取 `params.impedance`，DC/欧姆档取 `params.dcResistance`（§6.1）；
- `state`（可选）= 得电状态键（线圈类），可被 `bind`/`when` 引用；
- `computed`：`"motor"`（电机绕组双态阻抗）、`"reflect"`（整流器/变压器功率反射）；
- loads 在**短路判定图不合并**（负载自由）、在**带电检测图合并**（U1 接 L1 → U2 判带电）。

**③ `permanent` 恒连** —— 如同内部导线
```jsonc
"permanent": { "j": { "pairs": [["v-","v-_1"], ["v+","v+_1"]], "fault": { "level": 10 } } }
```

**④ `sensors` 电流测量支路** —— 串联测电流（热元件、电流表分流）
```jsonc
"sensors": { "shunt": { "pairs": [["t1","t2"]], "resistance": 0.02, "fault": { "level": 10 } } }
```
- 结构层恒连（不断路）；数值层按 `resistance` 产生电流；器件类可取 `solve.currentOf(inst, 'sensor:<key>:<对下标>')`。

---

## 八、故障配置原理（排故）

**① 条目级 `fault`**
```jsonc
"fault": { "level": 1, "types": ["open"] }
```
- `level`：**0 = 所有难度都可设（默认）；1 = 中等及以上；10/100 = 不设故障**；工具条难度按钮来自 `rules.faultLevels`，选中第 i 档 → 候选 `level ≤ i`；
- `types`：`"open"` = 断路（触点对断开 / loads·permanent·sensors 条目断开）、`"stuck"` = 粘连（仅 `contacts` 有意义：触点强制闭合）；
- 缺省：`contacts`/`loads` → level 0；`permanent`/`sensors` → level 10（不参与）。

**② 端子级 `faultLevel`**（导线故障）
```jsonc
"pins": { "t1_d": { "x": 36, "y": 57, "dir": "down", "faultLevel": 2 } }
```
- 只有断路；**导线故障等级 = 两端端子中更严格者（数值更大）**，`>1` 视为不设；
- `connectLimit: 0` 的端子不参与诊断。

**③ 候选与注入**
- 候选只在**已接线**的条目/导线上生成（空端子排没有故障可考）；
- 注入机制：断路 = 把该条目/导线从图里排除（`exclude`）或按条目类型断开（`isEntryOpen`）；粘连 = 给触点对强加一条恒连（`add`）；
- `level > 1` 条目的端子点击时提示"该端子不参与排故"。

**④ 案例：只让学生考"困难"档的电机绕组断路**
```jsonc
// components/electrical/three_phase_asynchronous_motor.json
"loads": { "u": { "pins": ["U1","U2"], "computed": "motor", "fault": { "level": 1, "types": ["open"] } } }
```
`rules.faultLevels: ["简单","中等","困难"]` → 该故障只在选中"中等"/"困难"时出现。

---

## 九、电源类器件（`source` / `input` / 功率反射）

### 9.1 `source` 电源 EMF

```jsonc
// 三相交流
"source": {
  "type": "ac", "voltage": 380,
  "phases": [
    { "pin": "l1", "angle": 0,    "label": "L1" },
    { "pin": "l2", "angle": 120,  "label": "L2" },
    { "pin": "l3", "angle": -120, "label": "L3" },
    { "pin": "n",  "angle": 0,    "label": "N", "magnitude": 0 }   // magnitude 缺省 = 相电压
  ]
}
// 直流输出
"source": { "type": "dc", "voltage": 24, "pins": { "v+": 1, "v-": 0 } }   // 值为电压系数 ×voltage
```
- `angle`（度）决定电机相序判定；`label` 参与相序标签；
- **输出电压可调**：`params.outputVoltage`（adjustable）优先；`magnitude` 随输出电压等比缩放；
- 单相输出（变压器次级）：两端显式写 `magnitude`（`{pin:"s1",angle:0,magnitude:220}` + `{pin:"s2",magnitude:0}`）；
- 电源内阻 = `params.sourceImpedance`（缺省 0.5Ω，短路限流用）。

### 9.2 `input` 输入判定（整流器/变压器）

```jsonc
"input": { "pins": ["l","n"], "voltage": 220, "tolerance": 0.15 }
```
- 两端同参考系 |ΔU| ∈ `voltage×(1±tolerance)` → 输入有效（**不满足则次级不带负载**）；`params.inputVoltage` 可调优先。

### 9.3 功率反射（`computed:"reflect"`）

整流器/变压器的初级＝功率反射支路：每 tick 由次级输出功率折算等效阻抗，上级回路能测到真实电流/功率
`Z_in = |V_in|² / conj(S_total)`，`S_total = 负载复功率 + 空载复功率`。

| params | 说明 |
|---|---|
| `noLoadImpedance` | 空载阻抗幅值（变压器 5000Ω、整流器 48400Ω） |
| `noLoadPowerFactor` | 空载功率因数（变压器 0.1 感性励磁；整流器 1 纯阻待机） |
| `minInputImpedance` | 折算阻抗钳位（缺省 20Ω，防重载爆表） |
| `dcResistance` | 初级绕组直流电阻（欧姆档/DC 岛用，与动态折算解耦） |

未工作（输入错/开关断开/短路锁死）→ 1GΩ 等效开路（上级读数为 0）；次级输出端子在 working 时注册为源，与其他电源混接自动判短路。

---

## 十、气路器件专属字段

### 10.1 气源 `pneumaticSource`
```jsonc
{ "pneumaticSource": { "outlets": ["out"] } }
```
params 有 `switch` 键 = 带开关气源（开关闭合才出气）；无 = 恒压气源；出气压力 = `params.pressure`（默认 0.8 MPa）。

### 10.2 调压阀 `regulator`
```jsonc
{ "regulator": { "in": "p1", "out": ["p2"] } }
```
出气 = `min(设定值, 进气压力)`（`params.setPressure` 右键可调）；指针动画用 `anims.rotate` 绑 `out_pressure`。

### 10.3 气缸 `pneumatic.cylinders`（单/双作用/多单元通吃）
```jsonc
"pneumatic": {
  "cylinders": [
    { "posKey": "position", "label": "推杆",
      "ports": [ { "id": "a", "target": 1 }, { "id": "b", "target": 0 } ],   // 1 个口 = 单作用；2 个 = 双作用
      "equalBehavior": 1,        // 双作用：两侧等压(>0)时目标位置（0/1；省略 = 保持当前）
      "noAirTarget": 0 }         // 全部断气时目标位置（0/1；省略 = 保持原位）
  ]
}
```
| 字段 | 语义 |
|---|---|
| `posKey` | 必填：位置参数键（0=缩回、1=伸出），动画 `source` 引用它 |
| `label` | 单元显示名（绑定下拉用；缺省 = posKey） |
| `ports` | `[{id, target}]`：该气口通气时的目标位置；**1 个口 = 单作用**（断气按 `noAirTarget` > 保持）、**2 个 = 双作用**（气压占优口决定目标） |
| `equalBehavior` | 双作用两侧等压时目标（省略 = 保持） |
| `noAirTarget` | 全部断气时的目标位置：**单作用弹簧复位 = 0**；升降缸抵消重力 = 0；省略 = 断气停在原位 |

位置按 `params.strokeTime`（行程时间，右键可调 = 节流调速）积分；能力 `relative-position` 暴露各单元位置给传感器/电机。

### 10.4 双线圈电磁阀 `pneumatic.valves`
```jsonc
"pneumatic": {
  "valves": [ { "type": "5_2_double", "posKeys": ["posA","posB"], "coilMap": { "coil1": "posA", "coil2": "posB" },
                "defaultPosKey": "posA", "inletPort": "p" } ]
}
```
- `posKeys` = 位置键（当前激活位置 true，气路 `contacts` 的 `when` 引用）；`coilMap` = 线圈条目 key → 该线圈得电的目标位；`defaultPosKey` = 复位位；`inletPort` = 进气端子（**有效触发 = 线圈得电 ∧ 进气有压**）；
- 状态机：双线圈都失电 → 保持（记忆）；双线圈同时得电 → 两位同时激活 + 告警栈提示；冲突态失电且有压 → 回 `defaultPosKey`。

### 10.5 单线圈阀（不进 `valves`）
线圈 = `loads` 条目（`state` 键）；气路切换 = `pneumatic contacts` 的 `when` 直接表达：
```jsonc
"contacts": { "a": { "pairs": [["p_in","a_out"]], "when": "!en3" }, "b": { "pairs": [["p_in","b_out"]], "when": "en3" } }
```

### 10.6 气路引擎行为（对配置的影响）
- 同一连通分量出现两种不同气压 → **串气告警**；有压分量上的未接线开口端子 → **漏气告警**（端子 💨 标记）；
- 排气口不建模：阀排气位 = 无压力源分量（0 MPa），因此**单作用缸断气即回弹簧位**；
- 带压端子禁止接线（与电气"带电禁止接线"同机制，`allowLiveConnect` 可豁免）；
- **气路永不设故障**：气路配置不写 `fault`/`faultLevel`（线圈故障写在电气 `loads` 上）。

---

## 十一、电动机（`MotorInstance`）

```jsonc
"params": [
  { "id": "coilVoltage", "default": 380 },                       // 额定线电压
  { "id": "ratedConnection", "label": "额定接法", "type": "select",
    "options": [{ "value": "Y", "label": "星形（Y）" }, { "value": "D", "label": "三角形（Δ）" }],
    "default": "Y", "adjustable": true },                        // 铭牌接法：决定额定相电压
  { "id": "minStartVoltage", "default": 0.4 },                    // 启动电压门槛（×额定相压）
  { "id": "balanceTol", "default": 0.1 },                         // 三相幅值平衡容差
  { "id": "phaseTol", "default": 10 },                            // 相位 120° 容差（度）
  { "id": "ratedPower", "label": "额定功率", "type": "number", "unit": "W", "default": 370, "adjustable": true },
  { "id": "powerFactor", "label": "功率因数", "type": "number", "default": 0.8, "adjustable": true },
  { "id": "efficiency", "label": "效率", "type": "number", "default": 0.85, "adjustable": true },
  { "id": "lockRatio", "label": "堵转电流倍数", "type": "number", "default": 6 },
  { "id": "ratedSpeed", "label": "额定转速", "type": "number", "unit": "rpm", "default": 1450, "adjustable": true },
  { "id": "accelTimeOn", "label": "通电加速时间", "type": "number", "unit": "s", "default": 1, "adjustable": true },
  { "id": "accelTimeOff", "label": "断电减速时间", "type": "number", "unit": "s", "default": 3, "adjustable": true },
  { "id": "ratedCurrent", "label": "额定电流", "type": "number", "unit": "A", "decimals": 2, "readonly": true },
  { "id": "stallCurrent", "label": "堵转电流", "type": "number", "unit": "A", "decimals": 2, "readonly": true },
  { "id": "target", "label": "堵转执行机构", "type": "bind", "allow": ["relative-position"], "adjustable": true },
  { "id": "unit", "label": "堵转单元", "type": "select", "options": "bound:target.relativePositions", "default": 0, "adjustable": true }
],
"terminals": { "electrical": { "loads": {
  "u": { "pins": ["U1","U2"], "computed": "motor" },   // 三相绕组：pins = [首端, 尾端]，3 条
  "v": { "pins": ["V1","V2"], "computed": "motor" },
  "w": { "pins": ["W1","W2"], "computed": "motor" } } } }
```

**运行判定原理**：每 tick 读三绕组电压相量（数值层电位），满足①幅值 ≥ `minStartVoltage`×额定相压 ②最大/最小幅值 ≤ `1+balanceTol` ③相位两两 120°±`phaseTol` → 判"能转"，相位旋转方向定正/反转。因此：
- **接法不必在配置里声明**：学生接成 Y 还是 Δ 由接线决定，电流差异由求解器自动算出（星三角启动、串电阻降压启动都能直接仿真）；
- `ratedConnection` 只影响**铭牌阻抗反推**（相电压不同 → 每相阻抗不同）；
- 绕组阻抗 = `3·U_相²·pf·η/P`（U_相 由 `ratedConnection` 决定），堵转阻抗 = 运转阻抗 / `lockRatio`；能转且未堵转用运转阻抗，否则用堵转阻抗（合闸第一拍即堵转阻抗 → 启动浪涌电流）。

**堵转**：`target` 绑定 `relative-position` 执行机构（如十字工作台）→ 方向顶到行程端时 `speed` 钳 0 并切堵转阻抗 → 电流 6 倍 → 热继电器延时跳闸（完整过载保护教学链）。

**速度继电器**（可选顶层字段，如 `motor_speed_relay.json`）：
```jsonc
"speedRelay": [ { "stateKey": "fwd_act", "dir": "pos" }, { "stateKey": "rev_act", "dir": "neg" } ]
```
转速 > `params.speedThreshold`（定值）→ `fwd_act` 置真；< −阈值 → `rev_act`；触点 `when` 引用这两个键（反接制动教学）。

---

## 十二、传感器与执行机构

### 12.1 磁性传感器 `MagneticSensorInstance`
```jsonc
"params": [
  { "id": "target", "label": "绑定气缸", "type": "bind", "allow": ["relative-position"], "adjustable": true },
  { "id": "unit", "label": "检测单元", "type": "select", "options": "bound:target.relativePositions", "default": 0, "adjustable": true },
  { "id": "detect", "label": "检测位置", "type": "number", "min": 0, "max": 1, "step": 0.01, "default": 1, "adjustable": true },
  { "id": "tolerance", "label": "到位容差", "type": "number", "default": 0.02 },
  { "id": "sensorType", "label": "类型", "type": "select", "options": [{"value":"npn","label":"NPN"},{"value":"pnp","label":"PNP"}], "default": "npn", "adjustable": true },
  { "id": "workingVoltage", "label": "工作电压", "type": "number", "unit": "V", "default": 24, "adjustable": true },
  { "id": "voltTol", "type": "number", "default": 0.1 }
]
```
工作条件 = 红/蓝同 DC 岛 ∧ 红高蓝低 ∧ |ΔU| ∈ `workingVoltage×(1±voltTol)`；到位 = `|position − detect| ≤ tolerance`；输出写 `out_npn`/`out_pnp`/`lit` 状态键（触点 `when` 引用）；接交流/超压会进告警栈。

### 12.2 行程开关 `LimitSwitchInstance`
机械触点、无供电：`target` + `unit` + `detect`（默认 1）+ `tolerance`（默认 0.05）；动作态 `actuated` = 位置触发 ∨ `forced`（面板热区强制）；NO `when:"actuated"` / NC `when:"!actuated"`。

### 12.3 十字工作台 `SlideTableInstance`（3D 悬浮视窗）
```jsonc
"params": [
  { "id": "xMotor", "label": "X轴电机", "type": "bind", "allow": ["speed"], "adjustable": true },
  { "id": "yMotor", "label": "Y轴电机", "type": "bind", "allow": ["speed"], "adjustable": true },
  { "id": "strokeTimeX", "label": "X行程时间@1000rpm", "type": "number", "unit": "s", "default": 10, "adjustable": true },
  { "id": "strokeTimeY", "label": "Y行程时间@1000rpm", "type": "number", "unit": "s", "default": 10, "adjustable": true },
  { "id": "x_pos", "type": "number", "default": 0.5, "display": { "x": 100, "y": 42, "prefix": "X ", "scale": 100, "suffix": "%", "color": "#58a6ff" } },
  { "id": "y_pos", "type": "number", "default": 0.5, "display": { "x": 100, "y": 68, "prefix": "Y ", "scale": 100, "suffix": "%", "color": "#3fb950" } }
]
```
- 双轴由绑定的电机 `speed` 积分（0~1，0.5 = 中点），行程时间 = 1000rpm 走完全程的秒数；
- 能力 `relative-position` → 被行程开关/磁性传感器检测，也被电机 `target` 绑定做堵转反馈；
- `zones` 里的 `view3d` 动作开关 3D 悬浮窗（单例器件）。

---

## 十三、场景组合与工程导入导出

场景（`scenes/*.json`）与工程快照共用"布局定义"：

```jsonc
{
  "id": "scene1", "name": "施耐德电机保护实训台", "description": "…",
  "layout": "duct",                              // 可选 "duct" | "free"（缺省按位置推断）
  "components": [
    { "id": "ac_power_380v_3phase", "row": 0, "left": 12, "x": 12, "y": 60,
      "params": { "sourceImpedance": 0.05, "target": "@1" },   // 参数/状态键同容器；bind 用 "@索引"
      "label": "电源QF1" }
  ],
  "wires": [
    { "from": [0, "l1"], "to": [1, "main_no_2_u"], "color": "#e74c3c", "media": "electrical",
      "label": "L1-1", "labelSide": "start" }      // label 线标；labelSide: both(缺省)/start/end
  ]
}
```

| 规则 | 行为 |
|---|---|
| 布局模式 | 显式 `layout` 优先；有 `row/left` → 有线槽；有 `x/y` → 无线槽；两者都有 → 沿用当前模式 |
| 有线槽 | 精确放置，冲突则向右 → 下行 → 自动加行 |
| 无线槽 | 精确放置，重叠取最近空位，空间不足按 `growFactor` 扩充 |
| 导线 | 一律自动敷设（气路恒悬链线）；`color` 可覆盖默认色；`media` 缺省 electrical |
| bind 引用 | 文件里写 `"@组件数组索引"`，放置完成后解析为运行期实例 |
| 失败回滚 | 任一环节失败 → 清空并提示原因 |

工具栏「📤 导出工程 / 📥 导入工程」；工程文件为全量快照（含状态键与名称）。

---

## 十四、能力协议（capabilities）

器件**能提供什么**用能力声明；**绑定**与跨器件取值按能力匹配，不依赖类名/继承。

```js
export class SpeedMeterInstance extends ComponentInstance {
  static capabilities = ['speed'];      // 声明能力
  getSpeed() { return this.params.speed || 0; }   // 实现契约方法
}
```

| 能力 | 契约 | 提供者 |
|---|---|---|
| `power` | `working`、`outputTerminals()`、`sourceEmf(term)` | 交流电源、整流器、变压器 |
| `speed` | `getSpeed()` → rpm | 电动机（含带速度继电器电机） |
| `trip` | `onShortCircuit(active)` | 断路器 |
| `pressure-source` | `working`、`outputTerminals()`、`pressureValue(term, inletP)` | 气源、调压阀 |
| `relative-position` | `relativePositions()` → `[{label,posKey,position}]` | 气缸、执行模块、十字工作台 |

- 契约注册表 = `js/components/capabilities.js` 的 `CAPABILITIES`（唯一事实来源），工厂实例化时自动校验，漏实现立即报错；
- `bind` 参数 `"allow": ["speed"]` → 面板只允许绑定具备该能力的器件；
- **新增能力**：在 `CAPABILITIES` 注册 + 器件类声明 + 实现契约方法（详见 `CLAUDE.md`）。

---

## 十五、内置器件类速查（`class` 字段可选值）

| class | 用途 | 关键配置 |
|---|---|---|
| `ComponentInstance` | 通用（开关/按钮/端子排/电阻/隔离手柄等"纯配置"器件） | `zones.state` + `contacts.when` 即可工作 |
| `CoilDeviceInstance` | 接触器 / 继电器 / 时间继电器 / 热继电器 | `params.coilVoltage`/`coilType`/`pickup`/`dropout`/`delayTime`/`heaterSetting`/`tripDelay` |
| `BreakerInstance` | 断路器（手动合闸 + 短路跳闸） | `zones.state:"switch"`；`trip` 能力自动跳闸 |
| `FuseInstance` | 熔断器（逐相电流熔断 + 复位热区） | `params.fuseCurrent`/`fuseTime`；`zones.action:"reset"` |
| `MotorInstance` | 三相异步电机（含速度继电器变体） | §十一 |
| `IndicatorInstance` | 指示灯（单灯/多灯模块） | `params.lampVoltage`/`lampType`/`impedance`/`dcResistance`；条目 `state` |
| `VoltmeterInstance` / `AmmeterInstance` / `ElectricMeterInstance` | 仪表（读数写 `data-mv`） | 端子接线即可 |
| `SpeedMeterInstance` | 指针式转速表 | `params.target`（bind speed） |
| `AcPowerInstance` / `RectifierInstance` / `TransformerInstance` | 电源 / 整流器 / 变压器 | `source`、`input`、功率反射（§九） |
| `PressureSourceInstance` / `PressureRegulatorInstance` | 气源 / 调压阀 | §十 |
| `PneumaticValveInstance` | 双线圈电磁阀/阀岛 | `pneumatic.valves` |
| `CylinderInstance` | 气缸（单/双作用、多单元） | `pneumatic.cylinders` |
| `MagneticSensorInstance` | 三线磁性接近开关 | `target`/`unit`/`detect`/`sensorType` |
| `LimitSwitchInstance` | 行程开关（机械触点） | `target`/`unit`/`detect`/`tolerance` |
| `SlideTableInstance` | 十字工作台（3D 悬浮视窗） | `xMotor`/`yMotor`/`strokeTimeX/Y` |

---

## 十六、配置案例速查（我要做 X → 这样配）

| 需求 | 做法 |
|---|---|
| 加一个自复位按钮 | `class: ComponentInstance` + `zones{event:"press",action:"toggle",state:"pushed"}` + `contacts` 的 NO `when:"pushed"` / NC `when:"!pushed"` |
| 加一个能流过电流的电阻（串电阻启动） | `loads` 条目 + `params.impedance`（数字，可调）；**不配 `dcResistance`** |
| 三相启动电阻箱（三组一起调） | 3 个 `loads` 条目 + 一个共享的 `params.impedance` |
| 加一个接线排（1 进 2 出） | 每列 3 个 `pins` + `permanent` 两对（上-中、中-下）+ 端子 `connectLimit:1` |
| 让器件图上的数值随参数变化 | `params[].display`（§4.5） |
| 让某个元素随参数移动/旋转 | `params[]` + `image[].anims`（§4.4） |
| 让导线跟着动端子走 | `anims.<标记>.terminal`（§4.4 例 B/D/E） |
| 换钮帽颜色/整套外观 | `params` select（值为 class 名）+ `image[].classBind` + CSS 类（§4.3） |
| 只显示、不给改的计算值 | `params[] { readonly:true }`，器件类里算好写入 params |
| 绑定另一台器件（如电机→工作台） | `params[] { type:"bind", allow:[能力名] }`（§6.2） |
| 只考"困难"档的某个断路故障 | 条目 `fault:{level:1,types:["open"]}`（§八） |
| 让某端子只能测不能接线 | `pins[].connectLimit: 0` |
| 允许带电接线（电源出线） | `pins[].allowLiveConnect: true` |
| 单作用气缸（弹簧复位/吸气保持） | `cylinders[].ports` 只写 1 个口；弹簧复位加 `noAirTarget: 0` |
| 升降缸断气自动下降 | `cylinders[].noAirTarget: 0` |
| 双线圈电磁阀（记忆/告警） | `pneumatic.valves` 单元（`coilMap` + `defaultPosKey` + `inletPort`） |
| 单线圈电磁阀 | 线圈 = `loads` 条目（`state`），气路切换用 `contacts.when` 表达 |
| 星三角启动教学 | 电机 `ratedConnection:"D"` + 学生接线成 Y/Δ 两态切换 |
| 电机堵转 + 热继电器保护 | 电机 `target` 绑工作台/执行机构；热继电器串主回路（`heaterSetting`/`tripDelay`） |
| 新建教学场景（含预接线） | 写 `scenes/<id>.json` + 加入主配置 `scenes` 数组（§十三） |

---

## 十七、完整示例（一个器件全字段）

```jsonc
{
  "id": "speed_relay", "class": "CoilDeviceInstance", "category": "继电器", "name": "速度继电器",
  "description": "转速达到阈值动作，用于反接制动",
  "tags": ["继电器", "速度"],
  "singleton": false,
  "size": { "width": 150, "height": 220 },
  "terminalHitRadius": 12,
  "image": [
    { "id": "default", "x": 0, "y": 0, "string": "<div><img src='images/速度继电器.png' width='150' height='220'></div>" },
    { "id": "动作", "x": 0, "y": 0, "bind": "energized", "string": "<div><img src='images/速度继电器_动作.png' width='150' height='220'></div>",
      "classBind": { "led": "ledColor" } }
  ],
  "zones": [
    { "id": "reset", "event": "click", "action": "ton_reset", "state": "tripped", "tips": "复位", "x": 120, "y": 40, "r": 14 }
  ],
  "params": [
    { "id": "ledColor", "label": "指示灯颜色", "type": "select", "adjustable": true, "default": "cap-red",
      "options": [ { "value": "cap-red", "label": "红" }, { "value": "cap-green", "label": "绿" } ] },
    { "id": "coilVoltage", "default": 220 },
    { "id": "coilType", "default": "ac", "adjustable": true },
    { "id": "impedance", "default": { "re": 730, "im": 6300 } },
    { "id": "dcResistance", "default": 730 },
    { "id": "threshold", "label": "动作转速", "type": "number", "unit": "rpm",
      "min": 10, "max": 6000, "step": 10, "default": 1200, "adjustable": true,
      "display": { "x": 75, "y": 200, "suffix": " rpm", "decimals": 0, "when": "threshold" } },
    { "id": "energized", "default": false },
    { "id": "name", "label": "名称", "type": "text", "default": "", "adjustable": true,
      "display": { "x": 75, "y": 20, "fontSize": 14, "class": "yellow-plate" } }
  ],
  "terminals": {
    "electrical": {
      "pins": {
        "a1": { "x": 20, "y": 40, "dir": "up", "tip": "A1线圈" },
        "a2": { "x": 20, "y": 180, "dir": "down", "tip": "A2线圈" },
        "nc_1_u": { "x": 110, "y": 40, "dir": "up", "tip": "常闭" },
        "nc_1_d": { "x": 110, "y": 180, "dir": "down", "tip": "常闭" }
      },
      "contacts": {
        "nc": { "pairs": [["nc_1_u", "nc_1_d"]], "when": "!energized", "closeDelay": 1, "openDelay": 0,
                "fault": { "level": 1, "types": ["open", "stuck"] } }
      },
      "loads": {
        "coil1": { "pins": ["a1", "a2"], "state": "energized", "fault": { "level": 1, "types": ["open"] } }
      },
      "permanent": {}, "sensors": {}
    }
  }
}
```
